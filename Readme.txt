Main function is at GeneratorController.php


copy.bat
- shell program to copy project folder to another folder (can have own database and project file - customize interface)


** Rmbr to put the location of copt.bat